version: '3'

services:
  go300_db:
    image: postgres
    ports:
      - 5432:5432

  go300:
    build: ./
    command: bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:80"
    ports:
      - 80:80
    depends_on:
      - go300_db
